[
  {
    "keywords": ["sheets", "s"],
    "description": "Sheet operations applied to each individual file",
    "commands": [
      {
        "keywords": ["add", "a"],
        "description": "Add a sheet to each individual files, defaults to end of file",
        "apply_to_all": true,
        "parameters": [
          {
            "name": "tile",
            "description": "Name of sheet to be added",
            "required": true
          },
          {
            "name": "location",
            "description": "Location to insert sheet",
            "required": false
          }
        ]
      },
      {
        "keywords": ["rename", "r"],
        "description": "Rename a sheet in all individual files",
        "apply_to_all": true,
        "parameters": [
          {
            "name": "old_title",
            "description": "Name of sheet to be renamed",
            "required": true
          },
          {
            "name": "new_title",
            "description": "New name of sheet",
            "required": true
          }
        ]
      },
      {
        "keywords": ["move", "m"],
        "description": "Move a sheet to specified location in all individual files",
        "apply_to_all": true,
        "parameters": [
          {
            "name": "tile",
            "description": "Name of sheet to be moved",
            "required": true
          },
          {
            "name": "location",
            "description": "Location to which sheet will be moved",
            "required": true
          }
        ]
      },
      {
        "keywords": ["delete", "d"],
        "description": "Delete a sheet from all individual files",
        "apply_to_all": true,
        "confirmation": true,
        "parameters": [
          {
            "name": "tile",
            "description": "Name of sheet to be deleted",
            "required": true
          }
        ]
      }
    ]
  },
  {
    "keywords": ["columns"],
    "description": "Column operations that affect all individual files",
    "commands": [
      {
        "keywords": ["insert"],
        "description": "Insert a column into specified sheet in all files, defaults to last column",
        "apply_to_all": true,
        "parameters": [
          {
            "name": "sheet",
            "description": "Sheet in which column will be inserted",
            "required": true
          },
          {
            "name": "column",
            "description": "Title of column to be inserted",
            "required": true
          },
          {
            "name": "position",
            "description": "Position in which column will be inserted",
            "required": false
          }
        ]
      },
      {
        "keywords": ["rename"],
        "description": "Rename specified column in all individual files",
        "apply_to_all": true,
        "parameters": [
          {
            "name": "sheet",
            "description": "Sheet in which column will be renamed",
            "required": true
          },
          {
            "name": "column",
            "description": "Name of column to be renamed",
            "required": true
          },
          {
            "name": "new_name",
            "description": "New name of column",
            "required": true
          }
        ]
      },
      {
        "keywords": ["move"],
        "description": "Move specified column in individual files to specified position",
        "apply_to_all": true,
        "parameters": [
          {
            "name": "sheet",
            "description": "Sheet in which column will be renamed",
            "required": true
          },
          {
            "name": "column",
            "description": "Name of column to be renamed",
            "required": true
          },
          {
            "name": "position",
            "description": "Position to which column would be moved",
            "required": true
          }
        ]
      },
      {
        "keywords": ["delete"],
        "description": "Delete specified column from individual files",
        "apply_to_all": true,
        "confirmation": true,
        "parameters": [
          {
            "name": "sheet",
            "description": "Sheet in which column will be deleted",
            "required": true
          },
          {
            "name": "column",
            "description": "Name of column to be deleted",
            "required": true
          }
        ]
      }
    ]
  }
]